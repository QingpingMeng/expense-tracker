class Category {
    name: string;
    icon: string;
    id: string;

    constructor(name: string, id: string, icon: string){
        this.name = name;
        this.icon = icon;
        this.id = id;
    }
}

export default Category;